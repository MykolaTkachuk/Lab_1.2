#include <stdio.h>
#include <stdlib.h>
int main()
{
 double delta, result, x1, x2;
 unsigned int N, variant;

 printf("Enter variant (1 or 2)");
 printf("\n Variant 1 means choosing count of columns \n Variant 2 means choosing step from first to second x:");
 scanf("%d",&variant);

    while(variant!= 1  &&  variant!= 2)
        {
        printf("Error. Please try again:");
        scanf("%d",&variant);
    }
      if(variant == 1)
        {
 printf("\n Enter x1: ");
 scanf("%lf",&x1);
 printf("\n Enter x2: ");
 scanf("%lf",&x2);
 printf("\n Enter N: ");
 scanf("%u",&N);
 int i=1;

 printf("\n************************************************");
 printf("\n*  N  *         X         *        F(X)        *");
 printf("\n************************************************");

    delta=(x2-x1)/(N-1);
    for(i;i<=N;i++)
    {
        result=2*x1*x1-3*x1*x1*x1+1500*x1-12000;
    printf("\n|%5.0d|%19.2f|%20.2f|\n",i,x1,result);

    if(i%10==0)

    {
        system("pause");

    }
    x1=x1+delta;
    }
    }

if(variant == 2){

 printf("\n Enter x1: ");
 scanf("%lf",&x1);
 printf("\n Enter x2: ");
 scanf("%lf",&x2);
 printf("\n  Enter delta:");
 scanf("%lf",&delta);
 int i=1;

 printf("\n************************************************");
 printf("\n*  N  *         X         *        F(X)        *");
 printf("\n************************************************");
    while(x1<=x2)
    {
          result=2*x1*x1-3*x1*x1*x1+1500*x1-12000;
    printf("\n|%5.0d|%19.2f|%20.2f|\n",i,x1,result);
    if(i%10==0)
    {

        system("PAUSE");



    }
    x1=x1+delta;
    i++;
    }
 }

    return 0;


}
